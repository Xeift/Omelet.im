//TODO:加好友的頁面

import 'package:flutter/material.dart';

class FriendsAddPage extends StatelessWidget {
  const FriendsAddPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child:Text('Friends Add'),
    );
  }
}